import Link from "next/link"

const Navbar = () => {
    return (
        <nav className="bg-gray-700">
            <div classnName="font-bold text-neutral-100 p-4 max-w-7xl mx-auto container tracking-widest">
            <i class="bi bi-arrow-left-short"></i>
            <a className="text-center>Configuracion">Configuration</a>
            </div>
        </nav>

    );
};

export default Navbar;